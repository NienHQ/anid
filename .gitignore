# Foundry
out/
cache/
# keep broadcast/ (deployed addresses) tracked

# Node / SDK (later)
node_modules/
dist/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp

# forge simulation artifacts (keep real broadcasts only)
**/dry-run/
